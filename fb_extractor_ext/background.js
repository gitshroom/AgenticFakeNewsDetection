// background.js
// Minimal; can be used for future auth or message routing. For now empty stub.
chrome.runtime.onInstalled.addListener(() => {
  console.log('FB Focus Extractor installed.');
});
